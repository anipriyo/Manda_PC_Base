#include<stdio.h>
#include<conio.h>
int main(){
    int n;
    printf("Enter the parameter n :");
    scanf("%d",&n);
    for (int i = 1; i <= n; i*=3)
    {
        for (int j = i; j <= n j++)
        {
            printf("Hi ");
        }
        
    }
    
    getch();
    return 0;
}